# Assets Folder

Place the tutor photo here as:

```
assets/tutor-photo.jpg
```

The page will automatically display it in the Tutor section. If the file is missing, a placeholder emoji is shown instead.
